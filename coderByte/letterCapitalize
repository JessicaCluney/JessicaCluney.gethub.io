function LetterCapitalize(str) { 
var newArray = [];
var arr = str.split(" ");
for(var i = 0; i < arr.length; i++) {
   newArray.push(arr[i][0].toUpperCase() + arr[i].slice(1));
}
return newArray.join(" ");
         
}
LetterCapitalize(readline()); //cap first letter of each words
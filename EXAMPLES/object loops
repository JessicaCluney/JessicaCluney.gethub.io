var obj = {
    one: 'a',
    two: 'b',
    three: 'c'
};
console.log(obj); //logs object

for(var key in obj) {
    console.log(key);//logs keys
}
for(var key in obj){
  console.log(obj[key]);//logs values
}